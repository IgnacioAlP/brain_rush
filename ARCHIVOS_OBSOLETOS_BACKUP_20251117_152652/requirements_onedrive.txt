# Dependencias necesarias para la integración con OneDrive

# Microsoft Authentication Library (MSAL) para Python
# Permite autenticación OAuth2 con Microsoft/Azure AD
msal>=1.24.0

# Requests ya debería estar instalado, pero aseguramos la versión
requests>=2.31.0

# Nota: Las siguientes dependencias ya deberían estar instaladas
# Si no están, ejecuta: pip install -r requirements.txt

# Flask y extensiones (ya instaladas)
# Flask>=3.0.0
# Flask-MySQLdb
# pymysql

# Para exportación a Excel (ya instalado)
# openpyxl>=3.1.0

# Para envío de correos (ya instalado)
# secure-smtplib

# Para variables de entorno (ya instalado)
# python-dotenv
